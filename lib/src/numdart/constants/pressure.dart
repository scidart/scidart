/// pressure in pascal
import 'length.dart';
import 'mass.dart';
import 'physical.dart';

const atm = 101325;
const atmosphere = atm;

const bar = 1e5;

const torr = atm / 760;
const mmHg = torr;

const psi = pound * g / (inch * inch);
