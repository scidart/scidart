import 'length.dart';
import 'time.dart';

// speed in meter per second

const kmh = 1e3 / hour;
const mph = mile / hour;
const mach = 340.5;
const speedOfSound = mach;
const knot = nauticalMile / hour;
