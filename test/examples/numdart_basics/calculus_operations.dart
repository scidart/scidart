import 'package:scidart/numdart.dart';
import 'package:test/test.dart';

void main() {
  test('calculus operations', () {
    var a = Array([-1, -1, -1, -1, 1, 1, 1, 1]);
    var ai = integration
  });
}