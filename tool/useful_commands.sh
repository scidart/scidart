#!/usr/bin/env bash

# generate doc
# dartdoc

# install http server
# pub global activate dhttpd

# open docs with http server
# dhttpd --path doc/api

# validate but do not publish the package.
# pub publish  --dry-run
# publish package
# pub publish